--- original_ai_output
+++ final_pattern.py
@@ -1,18 +1,40 @@
+"""AI-assisted motion pattern implementation.
+
+Preserve the original AI response in Streamlit. Review it, then implement a safe
+version here. The node accepts only segments returned by ``build_pattern``.
+"""
+from __future__ import annotations
+from dataclasses import dataclass
 import math
 
+@dataclass(frozen=True)
+class Segment:
+    linear_x: float
+    angular_z: float
+    duration: float
+
 def build_pattern(pattern_name: str) -> list[Segment]:
-if pattern_name != "alternating_arcs":
-raise ValueError(f"Unknown pattern name: {pattern_name!r}")
+    """Return ordered, bounded motion segments for the assigned pattern.
 
-radius = 0.30
-angular_speed = 0.70
-linear_speed = radius * angular_speed
-arc_angle = math.pi / 4.0
-duration = arc_angle / angular_speed
+    Supported assignments are ``rounded_rectangle``, ``l_path``, and
+    ``alternating_arcs``. Do not include the final stop; the ROS wrapper always
+    publishes it and the evaluator verifies it.
+    """
+    if pattern_name != "alternating_arcs":
+        raise ValueError(f"Unknown pattern name: {pattern_name!r}")
 
-return [
-Segment(linear_x=linear_speed, angular_z=angular_speed, duration=duration),
-Segment(linear_x=linear_speed, angular_z=-angular_speed, duration=duration),
-Segment(linear_x=linear_speed, angular_z=angular_speed, duration=duration),
-Segment(linear_x=linear_speed, angular_z=-angular_speed, duration=duration),
-]+    # Radius is fixed by the assignment; choose |omega| within the limit and
+    # derive v = R * |omega| so the arc radius is exact.
+    radius = 0.30            # m, required arc radius
+    omega = 0.70             # rad/s, <= 0.80 limit
+    v = radius * omega       # 0.21 m/s, <= 0.22 limit
+    turn = math.pi / 4.0     # 45 deg per arc
+    duration = turn / omega  # ~1.121997 s, <= 30 s; total ~4.488 s <= 60 s
+
+    return [
+        Segment(linear_x=v, angular_z=+omega, duration=duration),  # +45 left
+        Segment(linear_x=v, angular_z=-omega, duration=duration),  # -45 right
+        Segment(linear_x=v, angular_z=+omega, duration=duration),  # +45 left
+        Segment(linear_x=v, angular_z=-omega, duration=duration),  # -45 right
+    ]
+
